
import * as mocha from 'mocha';
import * as chai from 'chai';

describe('it', () => {
    it('add', () => {
        chai.assert.equal(1, 1);

    });

    it('sub', () => {
        chai.assert.equal(1, 2);

    });
});

