version: 0.1

environment_variables:
  DATABASE_URL: mysql://root:rootPass@localhost/ci_test

phases:
  install:
    commands:
      - npm install
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - npm test

artifacts:
  type: zip
  files:
    - test.js
